#!/bin/sh
git diff --cached --check || exit 1

if git diff --cached --name-only | grep -E '(^|/)\.env($|\.)' | grep -v '\.env\.example$'; then
  echo 'Arquivos de ambiente não devem entrar no commit.' >&2
  exit 1
fi
